import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App placeholder test', (WidgetTester tester) async {
    // TO DO: إضافة اختبارات فعلية
    expect(2 + 1, equals(3));
  });
}
