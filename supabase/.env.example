# نسخ هذا الملف إلى .env وملء القيم (لا ترفع .env إلى Git)
# Copy this file to .env and fill in values. Do not commit .env.
#
# كيف تستخدمه؟
# 1) انسخ: copy هذا الملف وسمّه .env (في نفس المجلد supabase أو في supabase/functions)
# 2) افتح .env وضَع رابط مشروعك والـ anon key والـ service role key مكان القيم الوهمية
# 3) محلياً: شغّل الدوال (استخدم npx لأن CLI مثبت في المشروع):
#    npx supabase functions serve create_child_account --env-file supabase/.env
#    - أو ضع .env داخل supabase/functions/.env (يُحمّل مع supabase start)
# 4) عند النشر: من لوحة Supabase (Edge Functions → Secrets) أضف SUPABASE_SERVICE_ROLE_KEY

SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
