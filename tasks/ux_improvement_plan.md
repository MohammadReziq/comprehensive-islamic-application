# خطة تحسين تجربة المستخدم الشاملة — صلاتي حياتي
## ولي الأمر · الابن · تدفق الدخول

> **الإصدار:** 3.0 — مراجعة شاملة وعميقة لجميع شاشات ولي الأمر والابن
> **النطاق:** كل شاشات ولي الأمر + الابن + تدفق الدخول + الأمان + البنية التقنية
> **الهدف:** تفكير شامل حرفياً بكل شاردة وواردة لتحويل التطبيق إلى تحفة في تجربة المستخدم

---

## 0. القضايا الحرجة (أمان وسلامة البيانات)

### 0.1 ثغرات أمنية (Security Issues)
- **تسريب كلمة مرور الابن:** الـ `ChildRepository` قد يعيد أو يخزن كلمة السر محلياً في حالة `ChildrenLoadedWithCredentials`. يجب نقل الـ credentials لـ SecureStorage فوراً والتخلص منها من الـ state.
- **إخفاء كلمة المرور:** في شاشة `AddChildScreen`، يجب إخفاء كلمة المرور (obscureText) مع زر إظهار العين.
- **صلاحية الربط:** غياب فحص RLS حقيقي يمنع أي ولي أمر من إضافة ابن لا يخصه إلى مسجد.

### 0.2 سلامة البيانات (Data Integrity)
- **سباق البيانات (Race Condition):** إنشاء `local_number` عند الإضافة لا يعتمد على Transaction أو Sequence، مما قد يسبب تكراراً لو أضاف وليان أبناء في ذات اللحظة للمسجد.
- **أبناء بدون حسابات (Orphaned Records):** إنشاء طفل مستفل (`user` في auth) وإدخاله في `children` لا يتم في Database Transaction. إذا فشلت خطوة، ستتلف البيانات.

### 0.3 أخطاء منطقية (Logic Bugs)
- **المكافآت (Streak Bonus):** شرط `if (streak == 7)` يُعطي المكافأة مرة واحدة فقط. يجب أن يكون `streak % 7 == 0`.
- **خطأ صامت في `HomeScreen`:** الـ BLoC قد يعود بـ error، لكن الـ UI لا يتعامل مع `ChildrenError` مما يترك شاشة فارغة محيرة لولي الأمر.
- **زر الناقص في العمر:** العمر يمكن أن ينزل لتحت 3 سنوات، يجب تعطيل زر `-` عند الوصول للحد الأدنى.
- **تحديث الإشعارات:** زر `MarkAllNotesRead` موجود في الـ BLoC ولكن الواجهة تفتقر لأي زر لاستدعائه.

---

## 1. شاشة "أبنائي" (ChildrenScreen)

### 1.1 المشاكل الحالية
- غياب حالة ارتباط الابن بالمسجد.
- لا يمكن تعديل بيانات الابن.
- لا توجد أداة للبحث والفلترة إذا كان عدد الأبناء كبيراً.

### 1.2 التحسينات الجذرية المضافة
1. **بطاقة حالة الارتباط (Smart Badges):** 
   - 🟢 مرتبط (مسجد النور).
   - 🔴 غير مرتبط (اضغط للربط).
2. **التوجيه الذكي (Smart Onboarding):**
   - إذا كان كل الأبناء "غير مرتبطين"، تظهر لافتة علوية بارزة (Banner) ترشد ولي الأمر بخطوات الربط.
3. **التمرير للتحديث (Pull-to-refresh):** إضافة `RefreshIndicator` لضمان الحصول على آخر تحديثات النقاط والمستوى.
4. **تعديل الابن السريع:** زر منبثق لتعديل الاسم/العمر دون الدخول العميل.

---

## 2. بطاقة الابن (ChildCardScreen)

### 2.1 المشاكل الحالية
- واجهة مضللة تعرض مربع "أدخل كود المسجد" دائماً.
- "طلب تصحيح" يظهر طوال الوقت.

### 2.2 التحسينات الجذرية المضافة
1. **ذكاء الواجهة (Conditional Rendering):**
   - 0 مساجد = يعرض فورم الربط بوضوح.
   - 1 مسجد = يعرض تفاصيل المسجد، زر مخفي "إضافة مسجد فرعي".
   - 2+ مساجد = قائمة مساجد قابلة للتوسيع.
2. **"طلب التصحيح" الدقيق:**
   - يظهر الـ Widget **فقط** إذا كان هناك مسابقة قيد التشغيل (`isRunning`).
3. **سجل سريع (Mini History):**
   - شريط سريع (Progress Bar) يعرض حضور آخر 7 أيام بشكل دوائر (✅❌).
4. **مشاركة الكود (Share QR):**
   - زر لمشاركة صورة الـ QR عبر الواتسآب للامام بشكل مباشر (`share_plus`).
5. **شريط التقدم البصري للمستوى (Level XP Bar):**
   - يعرض (النقاط الحالية / النقاط المطلوبة للمستوى القادم).

---

## 3. شاشة لوحة تحكم الابن (ChildViewScreen)

### 3.1 المشاكل الحالية
- مساحة مهدورة بشكل كبير.
- غياب عنصر المنافسة (Gamification) المتمثل في الشارات وترتيب قائمة الأوائل.

### 3.2 التحسينات الجذرية المضافة
1. **لوحة الشرف (Leaderboard):**
   - عرض الترتيب الحالي للابن داخل مسجده (باستخدام API الـ Gamification الموجود بالفعل).
   - إضاءة مميزة (Highlight) لمركز الابن.
2. **معرض الشارات الإنجازية (Badges Gallery):**
   - عرض الشارات المكتسبة مثل "بطل الفجر" وتظليل الشارات غير المكتسبة بالأبيض والأسود، وإضافة تأثير النقر لمعرفة كيفية كسبها.
3. **تقويم الحضور (Attendance Calendar):**
   - عرض التقويم الشهري الملون بأيام الحضور والمقصر فيها.

---

## 4. نظام الرسائل (Notes & Announcements)

### 4.1 المشاكل الحالية
- الشاشة تستخدم قائمة وهمية `[]` بدل استدعاء الـ `NotesBloc`.
- الإعلانات لا يتم تمييز المقروء منها عن الجديد.

### 4.2 التحسينات الجذرية المضافة
1. **ربط النظام (Integration):** تفعيل استهلاك `NotesBloc` وإنشاء شاشة رسائل حقيقية مجمعة حسب اسم الابن.
2. **نظام القراءة (Read Receipts):**
   - النقر على الإعلان أو الرسالة يسجل `read_at` في قاعدة البيانات ليعكس تغير اللون واختفاء العداد.
   - زر "تحديد الكل كمقروء" في أعلى الشاشة.
3. **تجميع الإعلانات (Grouping):** إذا كان الابن في أكثر من مسجد، تُصنف الإعلانات تحت ترويسة اسم المسجد المعني.

---

## 5. شاشة البروفايل والصحة وإعدادات الحساب (Profile & Settings)

### 5.1 المشاكل الحالية
- قسم `_ChildrenSection` غير مستخدم (Dead Code) في شجرة الـ UI.
- `_AccountHealthSection` يعاني من كارثة أداء `N+1 Query` (يستدعي قاعدة البيانات داخل حلقة تكرار لكل طفل).

### 5.2 التحسينات الجذرية المضافة
1. **تحديث قسم صحة الحساب:**
   - استبدال הـ N+1 Query بـ Batch Query لجلب بيانات وارتباط مساجد كل الأبناء في طلب واحد.
   - جعل التنبيهات قابلة للضغط (Actionable) لتوجيه المستخدم للحل مباشرة.
2. **إصلاحات عامة للبروفايل:**
   - تفعيل قسم الـ `_ChildrenSection` المخفي.
   - إضافة خيار **حذف الحساب نهائياً** للتوافق مع شروط المتاجر (Privacy).
   - إعدادات الإشعارات (إيقاف/تشغيل تنبيهات الحضور والرسائل).

---

## 6. خطة حذف الطالب (Soft Delete Framework)

**القرار النهائي:** اعتماد الحذف المنطقي (`Soft Delete`) لمنع تدمير إحصائيات الإمام وسجلات الحضور السابقة.

1. **الخطوة التقنية:** إضافة عمود `deleted_at (TIMESTAMP)` لجداول `children` و `user`.
2. **التعديل الشامل للاستعلامات:** تحديث كافة نقاط النهاية (Endpoints) ومستودعات البيانات (`Repositories`) لتجاهل القيود حيث `deleted_at IS NOT NULL`.
3. **الواجهة الأمامية (UI/UX):**
   - تصميم نافذة تأكيد خطرة (Red Zone Modal).
   - إرغام ولي الأمر على كتابة "اسم الابن" لتفعيل زر الحذف لتفادي الحذف بالخطأ.

---

## 7. خارطة الطريق للتنفيذ (Roadmap - مقسمة للإنتاجية القصوى)

| الدورة (Sprint) | التركيز | المهام الأساسية | الجهد التقديري |
|-----------------|---------|----------------|---------------|
| **Sprint 1** (الإنقاذ والإصلاح) | إصلاح الأخطاء القاتلة والأمان | إخفاء كلمات المرور، إصلاح الـ N+1 Query، وربط نظام الرسائل الميت (NotesInbox)، إظهار شريط الارتباط بمسجد. | 3 أيام |
| **Sprint 2** (تعديل الذكاء للواجهات) | المنطق الشرطي وبطاقات التوجيه | تعديل ظهور "نموذج الربط" و"طلبات التصحيح" برمجياً حسب حالة الطفل وحالة المسابقة. | يومان |
| **Sprint 3** (التلعيب والمكافآت) | شارات ومراكز الابن | استخدام Gamification API لإضافة Leaderboards و Badges ومشاركة QR Code. | يومان |
| **Sprint 4** (المعمارية وإدارة المخاطر) | حذف الابن وإدارة البيانات الجوهرية | إضافة الـ Soft Delete، تعديل قواعد البيانات وتصميم Validation Dialog. | 3 أيام |
| **Sprint 5** (صقل التجربة - الميزات الفاخرة) | إضافات ثانوية مبهرة | زر التحديد للكل كمقروء، عرض تقويم الحضور الشهري، خيار حذف حساب ولي الأمر. | يومان |

> ملحوظة: خارطة الطريق مصممة بحيث يبدأ المستخدم برؤية التحولات الضخمة منذ نهاية الدورة الأولى.
